/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { EmployerService } from "../employer.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { EmployerCreateInput } from "./EmployerCreateInput";
import { Employer } from "./Employer";
import { EmployerFindManyArgs } from "./EmployerFindManyArgs";
import { EmployerWhereUniqueInput } from "./EmployerWhereUniqueInput";
import { EmployerUpdateInput } from "./EmployerUpdateInput";
import { JobVacancyFindManyArgs } from "../../jobVacancy/base/JobVacancyFindManyArgs";
import { JobVacancy } from "../../jobVacancy/base/JobVacancy";
import { JobVacancyWhereUniqueInput } from "../../jobVacancy/base/JobVacancyWhereUniqueInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class EmployerControllerBase {
  constructor(
    protected readonly service: EmployerService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Employer })
  @nestAccessControl.UseRoles({
    resource: "Employer",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createEmployer(
    @common.Body() data: EmployerCreateInput
  ): Promise<Employer> {
    return await this.service.createEmployer({
      data: data,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        companyName: true,
        email: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [Employer] })
  @ApiNestedQuery(EmployerFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "Employer",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async employers(@common.Req() request: Request): Promise<Employer[]> {
    const args = plainToClass(EmployerFindManyArgs, request.query);
    return this.service.employers({
      ...args,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        companyName: true,
        email: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Employer })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Employer",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async employer(
    @common.Param() params: EmployerWhereUniqueInput
  ): Promise<Employer | null> {
    const result = await this.service.employer({
      where: params,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        companyName: true,
        email: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Employer })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Employer",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateEmployer(
    @common.Param() params: EmployerWhereUniqueInput,
    @common.Body() data: EmployerUpdateInput
  ): Promise<Employer | null> {
    try {
      return await this.service.updateEmployer({
        where: params,
        data: data,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
          companyName: true,
          email: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Employer })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Employer",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteEmployer(
    @common.Param() params: EmployerWhereUniqueInput
  ): Promise<Employer | null> {
    try {
      return await this.service.deleteEmployer({
        where: params,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
          companyName: true,
          email: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id/jobVacancies")
  @ApiNestedQuery(JobVacancyFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "JobVacancy",
    action: "read",
    possession: "any",
  })
  async findJobVacancies(
    @common.Req() request: Request,
    @common.Param() params: EmployerWhereUniqueInput
  ): Promise<JobVacancy[]> {
    const query = plainToClass(JobVacancyFindManyArgs, request.query);
    const results = await this.service.findJobVacancies(params.id, {
      ...query,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        title: true,
        description: true,

        employer: {
          select: {
            id: true,
          },
        },
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/jobVacancies")
  @nestAccessControl.UseRoles({
    resource: "Employer",
    action: "update",
    possession: "any",
  })
  async connectJobVacancies(
    @common.Param() params: EmployerWhereUniqueInput,
    @common.Body() body: JobVacancyWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      jobVacancies: {
        connect: body,
      },
    };
    await this.service.updateEmployer({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/jobVacancies")
  @nestAccessControl.UseRoles({
    resource: "Employer",
    action: "update",
    possession: "any",
  })
  async updateJobVacancies(
    @common.Param() params: EmployerWhereUniqueInput,
    @common.Body() body: JobVacancyWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      jobVacancies: {
        set: body,
      },
    };
    await this.service.updateEmployer({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/jobVacancies")
  @nestAccessControl.UseRoles({
    resource: "Employer",
    action: "update",
    possession: "any",
  })
  async disconnectJobVacancies(
    @common.Param() params: EmployerWhereUniqueInput,
    @common.Body() body: JobVacancyWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      jobVacancies: {
        disconnect: body,
      },
    };
    await this.service.updateEmployer({
      where: params,
      data,
      select: { id: true },
    });
  }
}
